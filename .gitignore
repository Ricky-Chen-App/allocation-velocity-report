# Dependencies
node_modules/

# Environment & secrets — NEVER commit
.env
*.env
!.env.example

# Runtime data (persisted locally / on /tmp in serverless)
data/member-profiles.json
data/org-chart.json
data/project-team.json

# OS / editor / local tooling
.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/
.claude/
.vercel
